# .prettierrc.yaml
# ✅ Prettier config สำหรับโปรเจกต์ Vite + React + TailwindCSS พร้อมใช้งานกับทีม dev และระบบ CI/CD

semi: false # ❌ ไม่ใส่ semicolon ท้ายบรรทัด
singleQuote: true # ✅ ใช้ single quote แทน double quote
trailingComma: es5 # ✅ ใส่ comma เฉพาะใน object/array ตามมาตรฐาน ES5
printWidth: 100 # ✅ จำกัดความยาว 1 บรรทัดไม่เกิน 100 ตัวอักษร
tabWidth: 2 # ✅ ขยับ indent ทีละ 2 space
useTabs: false # ✅ ใช้ space แทน tab
bracketSpacing: true # ✅ เว้นวรรคใน object literals เช่น { foo: bar }
arrowParens: avoid # ✅ ไม่ใส่วงเล็บหากพารามีตัวเดียว เช่น x => x
endOfLine: lf # ✅ ใช้ linefeed สำหรับความเข้ากันได้ข้าม platform

plugins:
  - prettier-plugin-tailwindcss # ✅ Plugin สำหรับจัดเรียง Tailwind classnames ให้อัตโนมัติ

tailwindConfig: './tailwind.config.ts' # ✅ ชี้ path ของ tailwind config เพื่อใช้ร่วมกับ plugin
